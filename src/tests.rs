#[cfg(test)]
mod tests {
    // Note this useful idiom: importing names from outer (for mod tests) scope.
    use super::*;

    // no functions to test yet!
    #[test]
    fn test_add() {
        assert_eq!(1+1, 3);
    }
}

